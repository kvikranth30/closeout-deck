# LLM API Access

Use your own LLM API key for this assignment. If you don't have access, let us know and we can provide one.

## Best Practices

1. **Cache responses** — Don't call the API for identical inputs
2. **Batch efficiently** — Process multiple shifts in one call where logical
3. **Handle errors gracefully** — Retry with backoff on rate limits
